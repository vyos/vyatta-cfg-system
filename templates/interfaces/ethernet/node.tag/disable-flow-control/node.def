priority: 320 # Must run after interface is configured.

help: Desabilitar controle de fluxo Ethernet (pause frames)

create: /sbin/ethtool --pause $VAR(../@) autoneg off tx off rx off

delete: [ -d /sys/class/net/$VAR(../@) ] || exit 0
	/sbin/ethtool --pause $VAR(../@) autoneg on tx on rx on
