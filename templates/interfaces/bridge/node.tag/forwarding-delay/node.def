type: u32
help: Delay de encaminhamento
syntax:expression:$VAR(@) <= 200; "Delay de encaminhamento deve ser entre 0 e 200"
val_help: u32:0-200; Delay de encaminhamento em segundos (padrao 15)
update: /sbin/brctl setfd $VAR(../@) $VAR(@)
