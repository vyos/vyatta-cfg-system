type: ipv4
help: Endereco de grupo Multicast para essa interface VXLAN
val_help: ipv4; Grupo Multicast para essa interface VXLAN

update:
  if [ ! -e /tmp/vxlan-$VAR(../@)-create ]; then
    echo "Chainging group requires delete/create this vxlan interface"
    exit 1
  fi
