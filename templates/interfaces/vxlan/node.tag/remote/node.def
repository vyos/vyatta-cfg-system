type: ipv4
help: Endereco remoto do tunel VXLAN
val_help: ipv4; Endereco remoto do tunel VXLAN

update:
  if [ ! -e /tmp/vxlan-$VAR(../@)-create ]; then
    echo "Chainging remote requires delete/create this vxlan interface"
    exit 1
  fi
