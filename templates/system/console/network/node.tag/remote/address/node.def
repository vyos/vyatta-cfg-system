type: ipv4
help: Endereco de IP remoto para esse console
val_help: ipv4; Endereco de IP remoto para esse console

update: NETCONS=/dev/config/netconsole/$VAR(../../@)
	sudo sh -c "echo $VAR(@) > $NETCONS/remote_ip"
