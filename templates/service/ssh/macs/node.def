type: txt
help: Especifica os algoritmos disponiveis MAC (codigo de autenticacao de mensagem). O algoritmo MAC e usado na versao 2 do protocolo para a protecao da integridade dos dados. Varios algoritmos devem ser separados por virgulas. Veja o 'man sshd_config' para Macs compativeis.

create: sudo sed -i -e '$ a \
MACs $VAR(@)' /etc/ssh/sshd_config

delete: sudo sed -i -e '/^MACs $VAR(@)$/d' /etc/ssh/sshd_config

update: sudo sed -i -e '/^MACs/c \
MACs $VAR(@)' /etc/ssh/sshd_config
