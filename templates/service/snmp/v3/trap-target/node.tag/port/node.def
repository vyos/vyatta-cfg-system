type: u32
default: 162
help: Especifica a porta TCP/UDP de destino para  SNMP traps/informs.

val_help: u32:1-65535; Numero de porta
syntax:expression: $VAR(@) > 0 && $VAR(@) <= 65535 ; \
	"Port number must be in range 1 to 65535"
