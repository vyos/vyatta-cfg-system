type: u32
help: Porta de destino utilizada para notificacao de trap

val_help: u32:1-65535; Numero da porta
syntax:expression: $VAR(@) > 0 && $VAR(@) <= 65535 ; \
	"Port number must be in range 1 to 65535"
